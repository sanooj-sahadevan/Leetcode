const dictionary = ["cat", "bat", "rat"],
  sentence = "the cattle was rattled by the battery";
// Output: "the cat was rat by the bat"
sentence.split("");
let results = [];

for (let i = 0; i < dictionary.length; i++) {
  if (sentence.includes(dictionary[i])) {
    
  }
}

